spring:
  datasource:
    url: jdbc:postgresql://supero:5432/rentcar
    username: postgres
    password: *****
  jpa:
    hibernate:
      ddl-auto: create-drop
  docker:
    compose:
      enabled: false
